PROJECT(cylvioncore)

INCLUDE_DIRECTORIES(include)
AUX_SOURCE_DIRECTORY(src cylvioncore_SOURCES)

ADD_LIBRARY(${PROJECT_NAME} SHARED ${cylvioncore_SOURCES})
INSTALL(TARGETS ${PROJECT_NAME}
  LIBRARY DESTINATION lib
)
