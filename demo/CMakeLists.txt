PROJECT(cylviondemo)

INCLUDE_DIRECTORIES(${cylvioncore_SOURCE_DIR}/include)

ADD_EXECUTABLE(${PROJECT_NAME} demo.c)
TARGET_LINK_LIBRARIES(${PROJECT_NAME} cylvioncore)
INSTALL(TARGETS ${PROJECT_NAME}
  DESTINATION bin
)
